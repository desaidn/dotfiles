# lazygit

Part of [dotfiles](../README.md).

## Dependencies

- LazyGit 0.56 or newer — provides the configured `git.pagers` interface
- Git
- Hunk 0.12 or newer — supplies the Diffing Solution through `hunk pager`
- Neovim — used through the shell-owned `EDITOR`/`VISUAL` contract
