# lazygit

Part of [dotfiles](../README.md).

## Dependencies

- [`difft`](https://difftastic.wilfred.me.uk/) — external diff viewer (`brew install difftastic`)
- Neovim with remote support — used as editor (`nvim-remote` preset)
